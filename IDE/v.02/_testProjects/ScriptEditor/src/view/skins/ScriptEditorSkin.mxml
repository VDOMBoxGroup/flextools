<?xml version="1.0" encoding="utf-8"?>
<s:Skin xmlns:fx="http://ns.adobe.com/mxml/2009"
		xmlns:mx="library://ns.adobe.com/flex/mx"
		xmlns:s="library://ns.adobe.com/flex/spark"
		xmlns:view="view.*"
		addedToStage="skin1_addedToStageHandler(event)" mouseDown="skin1_mouseDownHandler(event)" resize="skinnablecontainer1_resizeHandler(event)">

	<fx:Script>
		<![CDATA[
			import mx.events.ResizeEvent;

			protected function skinnablecontainer1_resizeHandler( event : ResizeEvent ) : void
			{
				ss.paint( width - vsb.width - 20, height - hsb.height - 5 );
			}

			protected function skin1_mouseDownHandler( event : MouseEvent ) : void
			{
				stage.focus = ss;
			}

			protected function skin1_addedToStageHandler( event : Event ) : void
			{
				ss.addEventListener( Event.SCROLL, ss_scrollHandler );

				updateScrollBar();
			}

			private function ss_scrollHandler( event : Event ) : void
			{
				updateScrollBar();
			}

			private function updateScrollBar() : void
			{
				vsb.maximum = ss.maxScrollV;
				vsb.value = ss.scrollV;

				hsb.maximum = ss.maxScrollH / 8;
				hsb.value = ss.scrollH / 8;
			}

			protected function hsb_changeHandler( event : Event ) : void
			{
				ss.scrollH = hsb.value * 8;
			}
		]]>
	</fx:Script>

	<s:states>
		<s:State name="normal"/>
		<s:State name="disabled"/>
	</s:states>


	<view:MyScriptAreaComponent id="ss"
								text="asdka ;lda sld"/>

	<s:HScrollBar id="hsb"
				  minimum="0"
				  bottom="0" left="0" right="{ vsb.width }"
				  change="hsb_changeHandler(event)"/>

	<s:VScrollBar id="vsb"
				  minimum="1"
				  bottom="{ hsb.height }" right="0" top="0"/>
</s:Skin>
