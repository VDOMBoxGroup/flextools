package connection
{
	import flash.events.EventDispatcher;
	import flash.events.IEventDispatcher;
	
	public class SOAPListLevel extends EventDispatcher
	{
		public function SOAPListLevel(target:IEventDispatcher=null)
		{
			super(target);
		}
	}
}